using System;
using System.Collections.Generic;

using Xamarin.Forms;
using Xamarin.Forms.Maps;

namespace MaraudeurMap {
    public partial class MapPage : ContentPage {
        public MapPage() {
            InitializeComponent();
            //var map = new Map(MapSpan.FromCenterAndRadius(new Position(86.2332, 10.2323), Distance.FromKilometers(10000)));
        }
    }
}
